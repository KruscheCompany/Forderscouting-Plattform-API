name: DEV DEPLOYMENT

on:
  push:
    branches: ["dev"]

jobs:
  build:
    runs-on:
      - self-hosted
      - dev
    defaults:
      run:
        working-directory: ${{ secrets.DEV_DEPLOY_PATH }}
    steps:
      - name: reset repo to HEAD and git pull
        run: git checkout dev && git reset --hard && git pull
      - name: Build dashboard in production
        run: NODE_ENV=production yarn build
      - name: import latest config sync
        run: yarn cs import --yes
      - name: restart service
        run: pm2 restart server
