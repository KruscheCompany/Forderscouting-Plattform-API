'use strict';

/**
 * funding-suggestion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::funding-suggestion.funding-suggestion');
