'use strict';

/**
 *  funding-suggestion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::funding-suggestion.funding-suggestion');
