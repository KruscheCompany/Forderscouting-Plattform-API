"use strict";

/**
 * vorpruefung-ticket service.
 */

const { createCoreService } = require("@strapi/strapi").factories;

module.exports = createCoreService("api::vorpruefung-ticket.vorpruefung-ticket");
